POS 页面三种订单类型，实际只有两种，不是三种，普通订单和常规订单一样，"普通订单"和"常规订单"的值都是 24，区别：仅影响信用评分中的提货时限

物流状态有 7 个阶段（定义在 app/Http/Helpers.php:1503），自动推进状态：app/Console/Kernel.php:81-88 中有一个每分钟执行的定时任务

计划任务：
你的系统通过 Laravel 调度器管理了以下任务（都在 app/Console/Kernel.php 中定义）：
┌────────────────────┬────────────┬──────────────────────────────────────────────────────────────────────┐
│        任务        │  执行频率  │                                 说明                                 │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ 查询汇率           │ 每天 23:00 │ queryExchangeRates 命令                                              │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ 释放冻结资金       │ 每分钟     │ 已确认收货的订单，冻结期到期后自动释放资金给卖家                     │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ 更新物流状态       │ 每分钟     │ 根据设定的物流时间线，自动推进 delivery_status（就是上一问说的那个） │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ 发送定时邮件       │ 每分钟     │ 从 email_tasks 表读取待发送邮件并发送                                │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ 自动增加店铺访问量 │ 每分钟     │ 给已审核店铺模拟增加访问量（随机间隔）                               │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ ~~生成店铺商品~~   │ 按需触发   │ 由 Redis key generate_shop_product 触发                              │
├────────────────────┼────────────┼──────────────────────────────────────────────────────────────────────┤
│ ~~执行翻译~~       │ 按需触发   │ 由 Redis key trigger_translate 触发                                  │
└────────────────────┴────────────┴──────────────────────────────────────────────────────────────────────┘
宝塔面板设置方法：进入 计划任务 页面，脚本内容：cd /www/wwwroot/你的项目目录 && php artisan schedule:run >> /dev/null 2>&1。 执行周期必须是 每分钟，因为 Laravel 调度器靠每分钟调用来判断各任务的触发时机，比如 5~15 分钟后显示，设置单位（秒）

仓库商品订单和普通商品订单：
普通商品订单（卖家自己上传的商品）走的是真实物流，卖家需要手动填写快递公司和运单号，没有自动模拟的物流轨迹。
至于如何区分——关键看订单里有没有 product_storehouse_total > 0，大于 0 的就是仓库商品订单。在订单列表里，这类订单会显示"提货金额"和"提货状态"列。

后台页面功能解释：
/admin/logistics_setting    这是一个模拟物流轨迹的配置。你的系统没有对接真实物流 API（如快递100），而是用 FedEx 作为虚拟快递公司，在订单支付完成后自动生成一条假的物流追踪信息。这个页面就是用来配置物流轨迹各阶段的时间间隔。
/admin/product_set_meal — 产品套餐（铺货套餐）这是一个"铺货加速包"：
/admin/general-setting 里的"原价比",这是管理员自己导入 Excel 时的折扣系数。导入数据时原价100，乘以这个比例，入库价格就是60。
/admin/seller_packages — 店铺等级（核心套餐），最大利润率（%） ← 这就是决定卖家售价的关键
/admin/seller_spread_packages — 店铺直通车，设置推广套餐
/admin/sellers 里的"设置套餐"，这是管理员手动给卖家分配"店铺等级"套餐（seller_packages），相当于管理员免费赠送卖家一个等级，不用卖家自己花钱买。
/admin/vendor_commission — 卖家佣金，这是平台抽成

商家页面功能解释：
/seller/commission-history 平台佣金：平台从这笔订单抽走的钱。收入：卖家这笔订单实际赚的钱

###### 卖家利润 = 订单总价(grand_total) - 提货成本(product_storehouse_total) - 平台佣金 - 优惠券

管理员 Excel 导入:
  仓库进价 = Excel原价 × original_price_ratio（如 100 × 0.6 = 60）

卖家导入仓库产品:
  卖家售价 = 仓库进价 × (1 + 店铺等级.max_profit%) （如 60 × 1.2 = 72）

客户下单:
  grand_total = 72
  product_storehouse_total（提货价）= 60
  平台佣金 = 72 × vendor_commission%（如 5%） = 3.6
  ─────────────────────────────────────────────
  卖家最终到手 = 72 - 60 - 3.6 = 8.4
  
具体例子

假设：
- 管理员仓库进价 = 60
- 卖家售价 = 72（进货价 60 × 1.2，利润率 20%）
- 后台佣金设置 = 5%
- 客户买 1 件

orderDetail.price  = 卖家折后售价 × 数量 = 72 × 1 = 72

平台佣金 = 72 × 5% = 3.6
卖家收入 = 72 + 税 + 运费 - 3.6

数据表（和商品库相关，product_id）：
product_stocks：SKU 变体，存每个规格的价格、库存量、variant 字符串（如 "S-红色"）、SKU 图片
product_translations：多语言翻译（名称、单位、描述）
product_taxes：每件商品的税率设置
wholesale_prices：批发/批量定价

order_details：订单明细，存每笔订单里买了什么产品、价格、数量、税
carts：购物车临时数据

reviews： 商品评价
wishlists： 用户收藏/心愿单
product_queries：用户对商品的咨询问答
conversations：客服对话（买家-卖家）
tickets：工单/支持票据
ticket_replies：客服工单消息内容表，和上面tickets表联动

flash_deal_products：限时秒杀关联
auction_product_bids：拍卖出价记录

卖家访问量设置：注册后有默认值，访问量随机秒数 1：最小间隔（秒），访问量随机秒数 2： 最大间隔（秒）。
审核通过并且店铺有商品的店铺才会按照计划任务去增长，店铺访问量每隔一段随机时间 +1。比如你设 100-500，那大概每 100~500 秒（约 1.5~8 分钟）自动涨 1 个访问量。

系统语言，代码里写好常规英文，用translate('Crypto Network')函数包括后，translate会自动处理拼接为crypto_network,加载页面时，函数会检测语言是否在语言包缓存中，不存在会自动添加到数据库，然后手动去后台进入相应的语言文件进行翻译，也可以配置自动翻译。常态语言包由redis缓存，一次性加载所有，不是每个都到数据库读取。

后台产品对话和买家咨询是同一个东西，同一个数据库。产品提问是独立的

清理方案推荐：
-- =============================================
-- 1. 删除卖家的 product_stocks（保留管理员的）
-- =============================================
DELETE FROM product_stocks
WHERE product_id IN (
    SELECT id FROM products WHERE original_id IS NOT NULL
);

-- =============================================
-- 2. 删除卖家复制的 products 记录
--    （级联关系会一并清理 product_translations 等）
-- =============================================
DELETE FROM products WHERE original_id IS NOT NULL;

-- =============================================
-- 3. 清空 product_taxes（会随重新导入重建）
-- =============================================
DELETE FROM product_taxes;

-- =============================================
-- 4. 清空批发价格
-- =============================================
DELETE FROM wholesale_prices;

-- =============================================
-- 5. 清空购物车（临时数据，stock_id 会变）
-- =============================================
DELETE FROM carts;

-- =============================================
-- 6. 清空翻译表（你已清空，确认一下）
-- =============================================
DELETE FROM product_translations;

新采集页面的路由是：

/ksldngsjdngkd/onebound-collect

另外之前写的 CJ 采集（CJdropshipping）页面路由是：

/ksldngsjdngkd/cj-collect

直接浏览器地址栏输入这两个 URL 就能访问，不需要菜单入口。如果需要加到菜单里，在后台的菜单管理（Menu 表）中添加对应路由即可。